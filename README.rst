Python Northbound API
=====================

Installation
------------

1. Go to project directory where the 'rest_controller' API is

$ ~/rest_controller

2. Install 'rest_controller' API

$ python setup.py install

That's all folks!!